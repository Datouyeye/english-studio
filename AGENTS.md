# AGENTS.md（gh-pages 线上站）

- 本仓库的 gh-pages 分支 = 线上网站，线上站点就是本目录下的单文件 `index.html`。
- 用户要求修改线上网站时，**优先处理 `index.html`**；不要默认修改 main 分支的 Next.js 项目。
- 以后所有线上网站修改都在 gh-pages 分支（本目录）进行。

## 省 token 规则
- 只读取当前任务需要的文件/片段；不读与任务无关的大文件（node_modules、lockfile、数据库、测试样例等）。
- 小改动先用精确搜索定位目标内容，避免整份文件反复进入上下文。
- 命令保持简短、一次到位；验证用最小手段（语法检查/局部检查），不启动重型服务。
- 需要上线时先与用户确认，再 commit + push；网络失败可重试。
